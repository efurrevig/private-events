<%= form_with(model: private_invite) do |form| %>
  <% if private_invite.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(private_invite.errors.count, "error") %> prohibited this private_invite from being saved:</h2>

      <ul>
        <% private_invite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
